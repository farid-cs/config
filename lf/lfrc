cmd touch &touch "$@"
cmd mkdir &mkdir -p "$@"
cmd open &open "$f"
cmd term &st

map . set hidden!
map D delete
map a push :touch<space>
map A push :mkdir<space>
map S fzf
map <c-t> &alacritty
map g. cd ~/.config
map gc cd ~/code
map gd cd ~/downloads
map gm cd ~/music
map <a-/> fzf
map t :term
